$PATH are added to .zshrc (configured)

for cacctoken:
sudo apt install xclip
put access-token.txt to ~/Downloads
or match it with the script
whatever

Install LAMP first, before using lampstatus
and all LAMP service alias already added in .zshrc
