from MSJCHE
https://www.youtube.com/watch?v=_wcrytoLufA

Install :
  i3lock-color (AUR)
  xidlehook (AUR)
  xautolock (REPO)
  imagemagick (REPO)
  scrot (REPO)
  dunst (REPO)
  libnotify (REPO)

Add ~/.local/bin to $PATH
  use ~/.zprofile

Write
  ~/.local/bin/lock
  ~/.local/bin/locker

Set 'locker' to run at startup

